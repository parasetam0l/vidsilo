# Postgres StatefulSet for the in-cluster database (single replica is
# sufficient for the VOD catalog; use an external DB for larger HA).
apiVersion: v1
kind: Service
metadata:
  name: vod-postgres
  labels:
    app: vod-postgres
spec:
  clusterIP: None
  selector:
    app: vod-postgres
  ports:
    - port: 5432
      name: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vod-postgres
  labels:
    app: vod-postgres
spec:
  serviceName: vod-postgres
  replicas: 1
  selector:
    matchLabels:
      app: vod-postgres
  template:
    metadata:
      labels:
        app: vod-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          env:
            - name: POSTGRES_USER
              value: vod
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: vod-secrets
                  key: db-password
            - name: POSTGRES_DB
              value: vod
          ports:
            - containerPort: 5432
              name: postgres
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "vod"]
            initialDelaySeconds: 5
            periodSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
