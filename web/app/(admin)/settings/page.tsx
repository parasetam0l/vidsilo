import { PagePlaceholder } from "@/components/page-placeholder"

export default function Page() {
  return <PagePlaceholder title="Settings" description="Grouped settings — general, storage, transcoding, analytics, playback, TLS" />
}
