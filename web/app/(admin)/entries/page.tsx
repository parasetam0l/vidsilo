import { PagePlaceholder } from "@/components/page-placeholder"

export default function Page() {
  return <PagePlaceholder title="Entries" description="Search (ILIKE), status/category filters, pagination, bulk delete" />
}
